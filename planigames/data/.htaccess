# JSON-Inhalte sind öffentlich (die Website lädt sie). Aber den Passwort-Hash,
# die Abonnentenliste (Datenschutz!) und temporäre Dateien nie ausliefern:
<FilesMatch "(auth\.php|users\.json|invites\.json|subscribers\.json|\.tmp|\.bak)$">
  Require all denied
</FilesMatch>
Options -Indexes
