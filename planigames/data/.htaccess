# JSON-Inhalte sind öffentlich (die Website lädt sie). Aber den Passwort-Hash
# und temporäre Schreibdateien nie ausliefern:
<FilesMatch "(auth\.php|\.tmp|\.bak)$">
  Require all denied
</FilesMatch>
Options -Indexes
